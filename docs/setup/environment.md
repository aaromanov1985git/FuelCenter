# Настройка переменных окружения

## Файлы для настройки

### 1. Для локальной разработки: `backend/.env`

Создайте файл `backend/.env` и добавьте следующие переменные:

```env
# Включить аутентификацию
ENABLE_AUTH=true

# Секретный ключ для JWT (ОБЯЗАТЕЛЬНО ИЗМЕНИТЬ В PRODUCTION!)
SECRET_KEY=your-very-secret-key-change-in-production-use-random-string

# Дополнительные настройки (опционально)
JWT_EXPIRE_MINUTES=30
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=admin@example.com
```

**Важно:** 
- Файл `.env` должен быть в директории `backend/`
- Файл `.env` уже добавлен в `.gitignore` и не будет попадать в git
- **В production обязательно смените SECRET_KEY на случайную строку!**

### 2. Для Docker: `.env` в корне проекта

Если используете Docker, создайте файл `.env` в **корне проекта** (рядом с `docker-compose.yml`):

```env
# Включить аутентификацию
ENABLE_AUTH=true

# Секретный ключ для JWT
SECRET_KEY=your-very-secret-key-change-in-production-use-random-string

# Ключ для шифрования чувствительных данных (пароли Firebird, API ключи)
# Если не указан, используется SECRET_KEY
# ОБЯЗАТЕЛЬНО ИЗМЕНИТЬ В PRODUCTION!
ENCRYPTION_KEY=your-encryption-key-here-change-in-production

# Настройки администратора (опционально)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=admin@example.com

# Настройки БД (если нужно изменить)
DATABASE_URL=postgresql://gsm_user:gsm_password@db:5432/gsm_db
POSTGRES_USER=gsm_user
POSTGRES_PASSWORD=gsm_password
POSTGRES_DB=gsm_db

# Настройки Rate Limiting (защита от DDoS)
ENABLE_RATE_LIMIT=true
RATE_LIMIT_DEFAULT=100/minute
RATE_LIMIT_STRICT=10/minute
```

Docker Compose автоматически подхватит эти переменные из файла `.env` в корне проекта.

### 3. Генерация безопасного SECRET_KEY

Для генерации безопасного секретного ключа можно использовать:

**Linux/Mac:**
```bash
openssl rand -hex 32
```

**Windows (PowerShell):**
```powershell
-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
```

**Python:**
```python
import secrets
print(secrets.token_urlsafe(32))
```

## Проверка настроек

После настройки переменных окружения:

1. **Перезапустите backend:**
   ```bash
   # Если локально
   cd backend
   uvicorn app.main:app --reload
   
   # Если через Docker
   docker-compose restart backend
   ```

2. **Проверьте, что аутентификация включена:**
   - Откройте браузер и перейдите на frontend
   - Должна появиться форма входа

3. **Войдите с учетными данными администратора:**
   - Username: `admin` (или значение из `ADMIN_USERNAME`)
   - Password: `admin123` (или значение из `ADMIN_PASSWORD`)

## Отключение аутентификации

Если нужно временно отключить аутентификацию:

```env
ENABLE_AUTH=false
```

Или просто не указывайте `ENABLE_AUTH` (по умолчанию `false`).

## Важные замечания

⚠️ **Безопасность:**
- Никогда не коммитьте файл `.env` в git
- В production используйте сложный случайный `SECRET_KEY`
- Смените пароль администратора после первого входа
- Используйте HTTPS в production

📝 **Примеры:**
- См. `backend/.env.example` для полного списка переменных
- См. `backend/AUTHENTICATION.md` для подробной документации по аутентификации
- См. `backend/RATE_LIMITING.md` для подробной документации по rate limiting

