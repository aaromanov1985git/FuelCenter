# 🧪 Руководство по запуску E2E тестов

**Дата:** 2025-12-26  
**Статус:** ✅ E2E тесты готовы к использованию

---

## 📋 Обзор

E2E (End-to-End) тесты проверяют полные пользовательские сценарии, взаимодействуя с реальным фронтендом через браузер.

**Важно:** E2E тесты требуют запущенного фронтенда на `http://localhost:3000`.

---

## ⚙️ Предварительные требования

### 1. Установка Playwright

```bash
# Внутри контейнера backend
docker exec gsm_backend pip install playwright
docker exec gsm_backend playwright install chromium
```

Или если запускаете локально:

```bash
pip install playwright
playwright install chromium
```

### 2. Запуск фронтенда

**Обязательно:** Перед запуском E2E тестов запустите фронтенд:

```bash
# В отдельном терминале
cd frontend
npm run dev
```

Фронтенд должен быть доступен на `http://localhost:3000`.

### 3. Настройка доступа из Docker (если нужно)

Если фронтенд запущен на хосте, а тесты в Docker контейнере, используйте:

```bash
# Windows/Mac
docker exec -e E2E_FRONTEND_HOST=host.docker.internal gsm_backend pytest tests/e2e/ -v

# Linux
docker exec -e E2E_FRONTEND_HOST=172.17.0.1 gsm_backend pytest tests/e2e/ -v
```

**Примечание:** Тесты автоматически пробуют несколько вариантов доступа к хосту:
- `host.docker.internal` (Windows/Mac)
- `172.17.0.1` (стандартный Docker gateway)
- `localhost` (если на одном хосте)

---

## 🚀 Запуск E2E тестов

### Все E2E тесты

**Если фронтенд запущен на хосте, а тесты в Docker:**

```bash
# Для Windows/Mac (использует host.docker.internal)
docker exec -e E2E_FRONTEND_HOST=host.docker.internal gsm_backend pytest tests/e2e/ -v

# Или просто (автоматически попробует несколько вариантов)
docker exec gsm_backend pytest tests/e2e/ -v
```

**Если фронтенд и тесты на одном хосте:**

```bash
docker exec gsm_backend pytest tests/e2e/ -v
```

### Конкретный тест

```bash
docker exec gsm_backend pytest tests/e2e/test_auth_flow.py::TestAuthFlow::test_login_logout_flow -v
```

### С подробным выводом

```bash
docker exec gsm_backend pytest tests/e2e/ -v -s
```

---

## 📊 Доступные E2E тесты

### 1. Тесты аутентификации (`test_auth_flow.py`)

| Тест | Описание |
|------|----------|
| `test_login_logout_flow` | Полный цикл: вход → использование → выход |
| `test_view_transactions_list` | Просмотр списка транзакций |
| `test_dashboard_loads` | Загрузка дашборда |

### 2. Тесты загрузки файлов (`test_file_upload.py`)

| Тест | Описание |
|------|----------|
| `test_file_upload_flow` | Полный поток загрузки файла |
| `test_transactions_list_view` | Просмотр списка транзакций |
| `test_providers_page` | Страница провайдеров |
| `test_vehicles_page` | Страница транспортных средств |

**Всего:** 7 E2E тестов

---

## 🔍 Проверка доступности фронтенда

E2E тесты автоматически проверяют доступность фронтенда перед запуском.

Если фронтенд недоступен, тесты будут пропущены с информативным сообщением:

```
SKIPPED [1] tests/e2e/test_auth_flow.py: Frontend недоступен на http://localhost:3000.
Запустите фронтенд перед выполнением E2E тестов:
  cd frontend && npm run dev
```

---

## 🐛 Решение проблем

### Ошибка: `ERR_CONNECTION_REFUSED` или тесты пропускаются

**Причина:** Фронтенд не запущен или недоступен из Docker контейнера.

**Решение:**

1. **Убедитесь, что фронтенд запущен:**
   ```bash
   cd frontend && npm run dev
   ```

2. **Если фронтенд на хосте, а тесты в Docker:**
   ```bash
   # Для Windows/Mac
   docker exec -e E2E_FRONTEND_HOST=host.docker.internal gsm_backend pytest tests/e2e/ -v
   
   # Для Linux (может потребоваться другой IP)
   docker exec -e E2E_FRONTEND_HOST=172.17.0.1 gsm_backend pytest tests/e2e/ -v
   ```

3. **Проверьте доступность:**
   ```bash
   # С хоста
   curl http://localhost:3000
   
   # Из контейнера
   docker exec gsm_backend curl http://host.docker.internal:3000
   ```

4. **Настройка через переменные окружения:**
   ```bash
   # Установите переменные окружения
   export E2E_FRONTEND_HOST=host.docker.internal
   export E2E_FRONTEND_PORT=3000
   
   # Запустите тесты
   docker exec -e E2E_FRONTEND_HOST=$E2E_FRONTEND_HOST \
              -e E2E_FRONTEND_PORT=$E2E_FRONTEND_PORT \
              gsm_backend pytest tests/e2e/ -v
   ```

### Ошибка: `Timeout`

**Причина:** Фронтенд слишком долго отвечает.

**Решение:**
1. Проверьте производительность фронтенда
2. Увеличьте таймауты в тестах (если необходимо)
3. Проверьте логи фронтенда на ошибки

### Ошибка: `Element not found`

**Причина:** Изменилась структура UI или селекторы устарели.

**Решение:**
1. Обновите селекторы в тестах
2. Используйте `data-testid` атрибуты для более стабильных селекторов

---

## 📝 Добавление новых E2E тестов

### Шаблон теста

```python
"""
E2E тест для [описание функциональности]
"""
import pytest
from playwright.sync_api import Page, expect


@pytest.mark.e2e
def test_example(page: Page):
    """Тест [описание]"""
    # 1. Переход на страницу
    page.goto("http://localhost:3000/login")
    
    # 2. Авторизация (если требуется)
    page.fill("input[name='username']", "admin")
    page.fill("input[name='password']", "admin")
    page.click("button[type='submit']")
    
    # 3. Выполнение действий
    page.goto("http://localhost:3000/target-page")
    
    # 4. Проверка результатов
    expect(page.locator("text=Ожидаемый текст")).to_be_visible()
```

### Рекомендации

1. **Используйте `data-testid`** для стабильных селекторов:
   ```python
   page.locator('[data-testid="submit-button"]')
   ```

2. **Добавляйте таймауты** для медленных операций:
   ```python
   expect(page).to_have_url("http://localhost:3000/dashboard", timeout=10000)
   ```

3. **Группируйте связанные тесты** в классы:
   ```python
   @pytest.mark.e2e
   class TestFeature:
       def test_feature_1(self, page: Page):
           ...
       
       def test_feature_2(self, page: Page):
           ...
   ```

---

## 🔄 Интеграция в CI/CD

### GitHub Actions пример

```yaml
name: E2E Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  e2e:
    runs-on: ubuntu-latest
    
    services:
      frontend:
        image: node:18
        ports:
          - 3000:3000
        # Запуск фронтенда
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install -r backend/requirements.txt
          pip install playwright
          playwright install chromium
      
      - name: Run E2E tests
        run: |
          cd backend
          pytest tests/e2e/ -v
```

---

## 📈 Статистика E2E тестов

| Категория | Количество | Статус |
|-----------|-----------|--------|
| Тесты аутентификации | 3 | ✅ Готовы |
| Тесты загрузки файлов | 4 | ✅ Готовы |
| **Всего** | **7** | ✅ **Готовы** |

---

## ✅ Чеклист перед запуском

- [ ] Playwright установлен (`pip install playwright`)
- [ ] Браузер установлен (`playwright install chromium`)
- [ ] Фронтенд запущен (`npm run dev`)
- [ ] Фронтенд доступен на `http://localhost:3000`
- [ ] Backend запущен и доступен
- [ ] База данных настроена

---

## 🎯 Следующие шаги

1. **Добавить больше E2E тестов** для критичных сценариев
2. **Настроить CI/CD** для автоматического запуска E2E тестов
3. **Добавить скриншоты** при падении тестов (Playwright поддерживает)
4. **Настроить параллельный запуск** для ускорения выполнения

---

**Дата создания:** 2025-12-26  
**Последнее обновление:** 2025-12-26

