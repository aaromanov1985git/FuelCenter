# Структура проекта для анализа адаптивности

## 🎯 Общая информация

**Проект:** GSM Converter (Конвертер ГСМ данных)  
**Frontend:** React 18 + Vite  
**Backend:** FastAPI (Python)  
**Текущий статус адаптивности:** Частично реализована (базовая поддержка мобильных устройств)

---

## 📁 Структура проекта

```
GSM/
├── frontend/ (React)
│   ├── src/
│   │   ├── components/          # Компоненты (46 компонентов)
│   │   │   ├── ui/              # UI-библиотека (11 компонентов)
│   │   │   │   ├── Button/
│   │   │   │   ├── Modal/
│   │   │   │   ├── Table/
│   │   │   │   ├── Input/
│   │   │   │   ├── Select/
│   │   │   │   ├── Badge/
│   │   │   │   ├── Card/
│   │   │   │   ├── Toast/
│   │   │   │   ├── Tooltip/
│   │   │   │   ├── Alert/
│   │   │   │   ├── Checkbox/
│   │   │   │   ├── Radio/
│   │   │   │   └── Skeleton/
│   │   │   ├── Dashboard.jsx    # Главная страница
│   │   │   ├── Login.jsx        # Авторизация
│   │   │   ├── Register.jsx     # Регистрация
│   │   │   ├── VehiclesList.jsx # Список транспорта
│   │   │   ├── GasStationsList.jsx # АЗС
│   │   │   ├── FuelCardsList.jsx   # Топливные карты
│   │   │   ├── TransactionsList.jsx # Транзакции
│   │   │   ├── ProvidersList.jsx    # Поставщики
│   │   │   ├── TemplatesList.jsx    # Шаблоны
│   │   │   ├── OrganizationsList.jsx # Организации
│   │   │   ├── UsersList.jsx        # Пользователи
│   │   │   └── ... (другие компоненты)
│   │   ├── contexts/            # React Context
│   │   │   └── AuthContext.jsx  # Контекст аутентификации
│   │   ├── hooks/               # Custom hooks (10 хуков)
│   │   │   ├── useTouchGestures.js  # Touch-жесты для мобильных
│   │   │   ├── useDebounce.js
│   │   │   ├── useFormValidation.js
│   │   │   └── ...
│   │   ├── styles/              # Глобальные стили
│   │   │   ├── animations.css
│   │   │   └── utilities.css
│   │   ├── utils/               # Утилиты
│   │   │   ├── api.js           # API клиент
│   │   │   ├── exportUtils.js
│   │   │   └── logger.js
│   │   ├── App.jsx              # Главный компонент
│   │   ├── App.css              # Главные стили (1600+ строк)
│   │   ├── index.css            # Глобальные стили (CSS переменные)
│   │   └── main.jsx             # Точка входа
│   ├── index.html
│   ├── vite.config.js
│   └── package.json
│
└── backend/ (FastAPI)
    ├── app/
    │   ├── routers/             # API роутеры (13 роутеров)
    │   ├── services/            # Бизнес-логика (18 сервисов)
    │   ├── repositories/        # Слой данных (4 репозитория)
    │   ├── models.py            # Модели БД
    │   ├── schemas.py           # Pydantic схемы
    │   └── main.py              # FastAPI приложение
    └── alembic/                 # Миграции БД
```

---

## 📱 Текущее состояние адаптивности

### ✅ Реализовано

1. **Breakpoints:**
   - Мобильные: `@media (max-width: 768px)`
   - Планшеты: `@media (min-width: 769px) and (max-width: 1024px)`
   - Маленькие экраны: `@media (max-width: 480px)`

2. **Мобильная навигация:**
   - Hamburger menu
   - Touch-жесты (swipe для открытия/закрытия сайдбара)
   - Overlay для закрытия меню
   - Адаптивный сайдбар (скрывается на мобильных)

3. **Адаптивные компоненты:**
   - Таблицы → карточный вид на мобильных
   - Модальные окна (max-height: 90vh, адаптивные отступы)
   - Touch-friendly кнопки (мин. 44px)
   - Адаптивная пагинация
   - Адаптивный дашборд (grid → 1 колонка)

4. **CSS переменные для адаптивности:**
   - Система отступов (8px grid)
   - Адаптивные размеры шрифтов
   - Адаптивные радиусы

### ⚠️ Требует доработки

1. **Таблицы:**
   - Минимальная ширина 1000px (проблема на маленьких экранах)
   - Горизонтальная прокрутка не оптимальна
   - Нет полноценной адаптации для всех типов таблиц

2. **Компоненты без полной адаптивности:**
   - TemplateEditor (сложный редактор)
   - AdvancedSearch (много полей)
   - FilePreviewModal (предпросмотр файлов)
   - CompactDashboard (может быть улучшен)

3. **Производительность:**
   - Нет оптимизации изображений
   - Нет lazy loading для компонентов
   - Большие CSS файлы (App.css ~1600 строк)

---

## 🎨 Система дизайна

### CSS переменные (index.css)

**Цветовая система:**
- Темы: `light` (по умолчанию), `dark` (через `data-theme="dark"`)
- CSS переменные для всех цветов
- Градиенты и состояния (hover, active)

**Типографика:**
- Размеры: `--font-size-xs` (12px) → `--font-size-4xl` (48px)
- Веса: `--font-weight-normal` (500) → `--font-weight-bold` (700)
- Высоты строк: `--line-height-tight` (1.2) → `--line-height-relaxed` (1.75)

**Отступы (8px grid system):**
- `--spacing-tiny`: 4px
- `--spacing-small`: 8px
- `--spacing-element`: 16px
- `--spacing-block`: 24px
- `--spacing-section`: 32px

**Радиусы:**
- `--radius-tiny`: 4px
- `--radius-small`: 6px
- `--radius-medium`: 8px
- `--radius-large`: 12px

---

## 🔍 Ключевые файлы для анализа

### Frontend

1. **src/App.jsx** (2232 строки)
   - Главная логика приложения
   - Обработка мобильных устройств (`isMobile` state)
   - Touch-жесты через `useTouchGestures`
   - Управление сайдбаром

2. **src/App.css** (~1600 строк)
   - Основные адаптивные стили
   - Media queries для всех breakpoints
   - Стили таблиц, модалок, навигации

3. **src/index.css** (276 строк)
   - CSS переменные (темы, цвета, отступы)
   - Базовые стили
   - Темная тема через `data-theme="dark"`

4. **src/components/ui/** (11 UI компонентов)
   - Переиспользуемая библиотека
   - Каждый компонент имеет свой CSS файл
   - Должны поддерживать адаптивность

5. **src/hooks/useTouchGestures.js**
   - Обработка swipe-жестов
   - Настройки: `minSwipeDistance: 50px`, `maxSwipeTime: 300ms`

### Компоненты со сложной адаптивностью

1. **src/components/VehiclesList.jsx** + `.css`
   - Большие таблицы с данными
   - Фильтры и поиск
   - Пагинация

2. **src/components/Dashboard.jsx** + `.css`
   - Дашборд с метриками
   - Grid-раскладка
   - Карточки статистики

3. **src/components/TemplateEditor.jsx** + `.css`
   - Сложный редактор шаблонов
   - Много полей ввода
   - Требует особой адаптации

4. **src/components/AdvancedSearch.jsx** + `.css`
   - Расширенный поиск
   - Множество фильтров
   - Нужна оптимизация для мобильных

---

## 📊 Метрики для анализа

### Компоненты
- **Всего компонентов:** ~46
- **UI компоненты:** 11
- **Страницы/Списки:** 10+
- **Модальные окна:** 8+

### CSS
- **App.css:** ~1600 строк (основные стили + адаптивность)
- **Отдельные CSS файлы:** 44 (по компонентам)
- **Глобальные стили:** 2 файла (index.css, utilities.css)

### Breakpoints
- **Мобильные:** ≤ 768px
- **Планшеты:** 769px - 1024px
- **Маленькие экраны:** ≤ 480px
- **Десктоп:** > 1024px

---

## 🎯 Приоритеты для адаптации

### Высокий приоритет
1. ✅ Таблицы (сейчас проблемная горизонтальная прокрутка)
2. ✅ TemplateEditor (сложный компонент)
3. ✅ AdvancedSearch (много полей)
4. ✅ Производительность (lazy loading, оптимизация)

### Средний приоритет
1. ⚠️ FilePreviewModal (предпросмотр)
2. ⚠️ Модальные окна (оптимизация для маленьких экранов)
3. ⚠️ Дашборд (компактный вид на мобильных)

### Низкий приоритет
1. ⚠️ Анимации (оптимизация для мобильных)
2. ⚠️ Tooltips (скрыты на мобильных, можно улучшить)

---

## 📝 Рекомендации для анализа

1. **Проверить все компоненты на:**
   - Корректную работу на экранах ≤ 768px
   - Touch-friendly размеры элементов (мин. 44px)
   - Отсутствие горизонтальной прокрутки (кроме таблиц)
   - Адаптивные отступы и размеры шрифтов

2. **Оптимизировать:**
   - Размеры CSS файлов
   - Использование CSS переменных (проверить, что нет хардкода)
   - Производительность рендеринга на мобильных

3. **Тестировать на:**
   - Разрешения: 320px, 375px, 414px, 768px, 1024px
   - Устройства: iPhone, Android, iPad
   - Браузеры: Chrome, Safari, Firefox

---

## 📚 Дополнительные документы

- `СТАНДАРТЫ_ДИЗАЙНА.md` - Стандарты дизайна и адаптивности
- `ПРАВИЛА_СТИЛИЗАЦИИ_И_ИМЕНОВАНИЯ.md` - Правила кодирования
- `TODO_DONE.md` - История выполненных задач по адаптивности
