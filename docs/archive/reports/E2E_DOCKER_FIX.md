# 🔧 Исправление доступа к фронтенду из Docker

**Дата:** 2025-12-26  
**Проблема:** E2E тесты не могут достучаться до фронтенда, запущенного на хосте

---

## 🐛 Проблема

Когда фронтенд запущен на хосте, а E2E тесты выполняются внутри Docker контейнера, `localhost:3000` из контейнера не указывает на хост.

**Симптомы:**
- Тесты пропускаются с сообщением "Frontend недоступен"
- Даже если фронтенд запущен на хосте

---

## ✅ Решение

### Автоматическое определение хоста

E2E тесты теперь автоматически пробуют несколько вариантов доступа к хосту:

1. `host.docker.internal` (Windows/Mac Docker)
2. `172.17.0.1` (стандартный Docker gateway)
3. `localhost` (если на одном хосте)
4. Значение из `E2E_FRONTEND_HOST` (если установлено)

### Ручная настройка

Если автоматическое определение не работает, установите переменную окружения:

```bash
# Windows/Mac
docker exec -e E2E_FRONTEND_HOST=host.docker.internal gsm_backend pytest tests/e2e/ -v

# Linux
docker exec -e E2E_FRONTEND_HOST=172.17.0.1 gsm_backend pytest tests/e2e/ -v

# Кастомный хост
docker exec -e E2E_FRONTEND_HOST=192.168.1.100 gsm_backend pytest tests/e2e/ -v
```

---

## 🔍 Проверка доступности

### С хоста

```bash
curl http://localhost:3000
```

### Из контейнера

```bash
# Проверка через host.docker.internal
docker exec gsm_backend curl http://host.docker.internal:3000

# Проверка через стандартный gateway
docker exec gsm_backend curl http://172.17.0.1:3000
```

---

## 📝 Изменения в коде

### `backend/tests/e2e/conftest.py`

**Добавлено:**
- Функция `is_frontend_available()` пробует несколько хостов
- Поддержка переменных окружения `E2E_FRONTEND_HOST` и `E2E_FRONTEND_PORT`
- HTTP проверка в дополнение к socket проверке

### `backend/tests/e2e/test_auth_flow.py` и `test_file_upload.py`

**Изменено:**
- Все жестко закодированные URL заменены на использование `base_url` фикстуры
- `base_url` использует переменные окружения для настройки

---

## 🚀 Использование

### Вариант 1: Автоматическое определение (рекомендуется)

```bash
# Просто запустите тесты - они попробуют найти фронтенд автоматически
docker exec gsm_backend pytest tests/e2e/ -v
```

### Вариант 2: Явное указание хоста

```bash
# Windows/Mac
docker exec -e E2E_FRONTEND_HOST=host.docker.internal gsm_backend pytest tests/e2e/ -v

# Linux
docker exec -e E2E_FRONTEND_HOST=172.17.0.1 gsm_backend pytest tests/e2e/ -v
```

### Вариант 3: Через docker-compose

Добавьте в `docker-compose.yml`:

```yaml
backend:
  environment:
    E2E_FRONTEND_HOST: host.docker.internal
    E2E_FRONTEND_PORT: 3000
```

---

## 🎯 Результат

**До исправления:**
```
SKIPPED - Frontend недоступен на http://localhost:3000
```

**После исправления:**
```
PASSED - Тесты успешно находят фронтенд через host.docker.internal
```

---

## 📚 Дополнительная информация

- Подробное руководство: `E2E_TESTS_GUIDE.md`
- Статус E2E тестов: `E2E_TESTS_STATUS.md`

---

**Дата:** 2025-12-26  
**Статус:** ✅ Исправлено

