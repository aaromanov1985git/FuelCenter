# План реализации: Анализ топливных карт

## 🎯 Цель проекта (этап 1)

Создать единый аналитический контур топливных расходов на основе сопоставления:
- **Транзакций по топливным картам** (уже реализовано)
- **Данных из системы мониторинга "Автограф" от Техноком** (готовим интеграцию)

**Фокус этапа 1:**
- Подтверждение заправок через телематику
- Привязка транзакций к ТС
- Расчёт пробега между заправками
- Выявление расхождений между транзакциями и фактическими заправками

## 📊 Текущее состояние

### ✅ Реализовано
- [x] Модели данных для транзакций (`Transaction`)
- [x] Модели для заправок ТС (`VehicleRefuel`)
- [x] Модели для местоположений ТС (`VehicleLocation`)
- [x] Модели для результатов анализа (`FuelCardAnalysisResult`)
- [x] Сервис анализа топливных карт (`FuelCardAnalysisService`)
- [x] API эндпоинты для анализа
- [x] Frontend компоненты для просмотра результатов
- [x] Механизмы получения транзакций от АЗС

### 🔄 В процессе
- [ ] Интеграция с системой мониторинга "Автограф Техноком"
- [ ] Перенос адаптера "Автограф" из другого проекта
- [ ] Автоматизация сопоставления транзакций и телеметрии

### ⏳ Планируется
- [ ] Интеграция с 1С (будущий этап)
- [ ] Интеграция с другими системами мониторинга (Wialon, GLONASS и др.)

## 🏗️ Архитектура решения

```
┌─────────────────────┐     ┌──────────────────────┐
│ Транзакции от АЗС  │     │ Система "Автограф"  │
│   (реализовано)     │     │   (готовим)         │
└──────────┬──────────┘     └──────────┬──────────┘
           │                           │
           ▼                           ▼
   ┌─────────────────────────────────────────┐
   │   Унифицированное ядро данных           │
   │   (единая модель события)               │
   └─────────────────────────────────────────┘
                      │
                      ▼
        ┌─────────────────────────────┐
        │  Модуль сопоставления       │
        │  по времени/ТС              │
        └─────────────────────────────┘
                      │
                      ▼
        ┌─────────────────────────────┐
        │  Обогащённые транзакции     │
        │  (enriched_fuel_transactions)│
        └─────────────────────────────┘
```

## 📋 План задач по этапам

### Этап 1.1: Подготовка инфраструктуры для интеграции

#### Задача 1.1.1: Создать унифицированную модель данных
**Цель:** Абстрагировать различия между системами мониторинга

**Действия:**
- [ ] Создать базовый интерфейс `IMonitoringAdapter`
- [ ] Определить унифицированную модель `TelemetryEvent`
- [ ] Подготовить схемы данных для хранения телеметрии

**Рекомендации для Cursor AI:**
```
Создай Python-класс TelemetryEvent с полями: vehicle_id, timestamp, 
lat, lon, odometer_km, event_type, source_system. Добавь Pydantic-модель 
для валидации и сериализации.
```

**Файлы:**
- `backend/app/models/telemetry_event.py` (новый)
- `backend/app/schemas/telemetry_schemas.py` (новый)

---

#### Задача 1.1.2: Создать справочник "Карта → ТС" с историей
**Цель:** Корректная привязка транзакций к транспортным средствам

**Действия:**
- [ ] Создать модель `CardVehicleAssignment` с полями:
  - `card_id` (FK к FuelCard)
  - `vehicle_id` (FK к Vehicle)
  - `valid_from` (дата начала)
  - `valid_to` (дата окончания, NULL = активна)
  - `assigned_by` (кто назначил)
- [ ] Создать миграцию БД
- [ ] Создать API для управления привязками
- [ ] Создать frontend для управления привязками

**Рекомендации для Cursor AI:**
```
Создай SQLAlchemy модель CardVehicleAssignment с поддержкой историчности.
Добавь метод get_active_vehicle_for_card(card_id, date) для получения 
активного ТС на конкретную дату.
```

**Файлы:**
- Обновить `backend/app/models.py`
- `backend/alembic/versions/YYYYMMDD_add_card_vehicle_assignment.py`
- `backend/app/routers/card_vehicle_assignment.py` (новый)
- `src/components/CardVehicleAssignment.jsx` (новый)

---

### Этап 1.2: Интеграция с "Автограф Техноком"

#### Задача 1.2.1: Перенос адаптера "Автограф"
**Цель:** Использовать готовые методы из другого проекта

**Действия:**
- [ ] Найти и изучить существующий код адаптера
- [ ] Создать модуль `backend/app/adapters/autograph_adapter.py`
- [ ] Реализовать интерфейс `IMonitoringAdapter`:
  - `get_positions(vehicle_id, start, end)` → список местоположений
  - `get_odometer(vehicle_id, timestamp)` → пробег на момент
  - `get_refuel_events(vehicle_id, period)` → фактические заправки
- [ ] Добавить обработку ошибок и retry-логику
- [ ] Добавить логирование запросов

**Рекомендации для Cursor AI:**
```
Создай класс AutographAdapter, реализующий IMonitoringAdapter. 
Используй requests.Session для переиспользования соединений. 
Добавь обработку таймаутов, retry с экспоненциальной задержкой, 
и преобразование ответов API в унифицированный формат TelemetryEvent.
```

**Файлы:**
- `backend/app/adapters/__init__.py` (новый)
- `backend/app/adapters/base_adapter.py` (новый - интерфейс)
- `backend/app/adapters/autograph_adapter.py` (новый)

**Конфигурация:**
- Добавить настройки в `backend/app/config.py`:
  - `AUTOGRAPH_BASE_URL`
  - `AUTOGRAPH_USERNAME`
  - `AUTOGRAPH_PASSWORD`
  - `AUTOGRAPH_TIMEOUT`
  - `AUTOGRAPH_RETRY_COUNT`

---

#### Задача 1.2.2: Тестирование адаптера
**Цель:** Убедиться в корректной работе с API "Автограф"

**Действия:**
- [ ] Создать тестовый скрипт для проверки подключения
- [ ] Протестировать получение данных за тестовый период
- [ ] Проверить обработку ошибок (недоступность API, неверные credentials)
- [ ] Задокументировать особенности API (лимиты, форматы дат и т.д.)

**Файлы:**
- `backend/scripts/test_autograph_connection.py` (новый)

---

### Этап 1.3: Модуль сопоставления транзакций и телеметрии

#### Задача 1.3.1: Реализовать алгоритм сопоставления
**Цель:** Обогатить транзакции данными из телематики

**Действия:**
- [ ] Создать сервис `TransactionEnrichmentService`
- [ ] Реализовать функцию сопоставления:
  - Для каждой транзакции найти привязанное ТС
  - Запросить телеметрию за период ±15 минут от времени транзакции
  - Найти ближайшее событие по времени
  - Проверить наличие фактической заправки в телематике
  - Рассчитать расстояние от ТС до АЗС (если есть координаты)
- [ ] Сохранить результат в `enriched_fuel_transactions`

**Рекомендации для Cursor AI:**
```
Создай функцию match_transaction_with_telemetry(transaction, telemetry_events) 
которая находит ближайшее событие в окне ±15 минут. Используй минимальную 
временную разницу. Верни обогащённый объект с полями: refuel_confirmed, 
distance_from_azs_m, telemetry_lat, telemetry_lon, odometer_km.
```

**Файлы:**
- `backend/app/services/transaction_enrichment_service.py` (новый)
- Обновить `backend/app/services/fuel_card_analysis_service.py`

---

#### Задача 1.3.2: Создать таблицу для обогащённых транзакций
**Цель:** Хранить результаты сопоставления для аналитики

**Действия:**
- [ ] Создать модель `EnrichedFuelTransaction`
- [ ] Создать миграцию БД
- [ ] Добавить индексы для быстрого поиска

**SQL схема:**
```sql
CREATE TABLE enriched_fuel_transactions (
    id UUID PRIMARY KEY,
    transaction_id INTEGER REFERENCES transactions(id),
    card_id INTEGER REFERENCES fuel_cards(id),
    vehicle_id INTEGER REFERENCES vehicles(id),
    transaction_ts TIMESTAMPTZ NOT NULL,
    volume_l NUMERIC(10,2),
    amount_rub NUMERIC(10,2),
    azs_name TEXT,
    azs_lat NUMERIC(10,8),
    azs_lon NUMERIC(11,8),
    telemetry_lat NUMERIC(10,8),
    telemetry_lon NUMERIC(11,8),
    distance_from_azs_m NUMERIC(10,2),
    odometer_km NUMERIC(10,1),
    refuel_confirmed BOOLEAN DEFAULT FALSE,
    time_diff_seconds INTEGER,
    matching_status TEXT, -- 'matched', 'no_vehicle', 'no_telemetry', 'ambiguous'
    matching_details JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_enriched_transaction_ts ON enriched_fuel_transactions(transaction_ts);
CREATE INDEX idx_enriched_vehicle ON enriched_fuel_transactions(vehicle_id);
CREATE INDEX idx_enriched_status ON enriched_fuel_transactions(matching_status);
```

**Файлы:**
- Обновить `backend/app/models.py`
- `backend/alembic/versions/YYYYMMDD_add_enriched_transactions.py`

---

#### Задача 1.3.3: Создать аудит-лог сопоставления
**Цель:** Отслеживать качество сопоставления и находить проблемы

**Действия:**
- [ ] Создать модель `FuelMatchingLog`
- [ ] Логировать каждую попытку сопоставления:
  - Статус (matched, no_vehicle, no_telemetry, ambiguous)
  - Детали (количество кандидатов, лучшая временная разница)
  - Ошибки (если были)

**Файлы:**
- Обновить `backend/app/models.py`
- `backend/alembic/versions/YYYYMMDD_add_matching_log.py`

---

### Этап 1.4: Автоматизация процесса

#### Задача 1.4.1: Создать фоновую задачу для сопоставления
**Цель:** Автоматически обрабатывать новые транзакции

**Действия:**
- [ ] Создать задачу для APScheduler:
  - Запускается ежечасно/ежедневно
  - Находит новые транзакции без обогащения
  - Выполняет сопоставление
  - Сохраняет результаты
- [ ] Добавить обработку ошибок и повторные попытки
- [ ] Добавить метрики (сколько обработано, сколько ошибок)

**Рекомендации для Cursor AI:**
```
Создай функцию process_new_transactions(date_from, date_to) которая:
1. Получает транзакции без обогащения за период
2. Для каждой транзакции вызывает enrichment service
3. Сохраняет результаты в enriched_fuel_transactions
4. Логирует статистику в fuel_matching_log
5. Обрабатывает ошибки с retry
```

**Файлы:**
- `backend/app/services/enrichment_scheduler.py` (новый)
- Обновить `backend/app/services/scheduler_service.py`

---

#### Задача 1.4.2: Создать API для ручного запуска обогащения
**Цель:** Возможность запустить обработку вручную

**Действия:**
- [ ] Добавить эндпоинт `POST /api/v1/fuel-card-analysis/enrich`
- [ ] Параметры: `date_from`, `date_to`, `card_ids` (опционально)
- [ ] Возвращать статистику обработки

**Файлы:**
- Обновить `backend/app/routers/fuel_card_analysis.py`

---

### Этап 1.5: Валидация и отладка

#### Задача 1.5.1: Создать дашборд качества сопоставления
**Цель:** Мониторить эффективность сопоставления

**Действия:**
- [ ] API эндпоинт для статистики:
  - Процент успешных сопоставлений
  - Распределение по статусам
  - Топ проблемных транзакций
- [ ] Frontend компонент для визуализации

**Файлы:**
- Обновить `backend/app/routers/fuel_card_analysis.py`
- `src/components/EnrichmentStats.jsx` (новый)

---

#### Задача 1.5.2: Создать ежедневный отчёт
**Цель:** Автоматические уведомления о результатах анализа

**Действия:**
- [ ] Создать задачу для генерации отчёта
- [ ] Формат: краткая статистика + список подозрительных транзакций
- [ ] Отправка через существующий механизм уведомлений (Telegram/email)

**Рекомендации для Cursor AI:**
```
Создай функцию generate_daily_fuel_report(date) которая формирует 
текстовый отчёт с метриками: всего транзакций, подтверждено, 
подозрительные. Используй существующий сервис уведомлений для отправки.
```

**Файлы:**
- `backend/app/services/fuel_report_service.py` (новый)

---

## 🔧 Технические рекомендации

### Приоритеты реализации

| Задача | Приоритет | Комментарий |
|--------|-----------|-------------|
| Перенос адаптера "Автограф" | 🔴 Высокий | Ядро интеграции |
| Справочник "Карта → ТС" | 🔴 Высокий | Критично для сопоставления |
| Модуль сопоставления | 🔴 Высокий | Главная ценность этапа |
| Унифицированная модель | 🟠 Средний | Инвестиция в будущее |
| Хранилище обогащённых данных | 🟠 Средний | Подготовка к визуализации |
| Автоматизация | 🟠 Средний | После валидации данных |
| Алерты / отчёты | 🟢 Низкий | Реализуем после MVP |

### Рекомендации по коду

1. **Используйте временные метки в UTC** — избегайте проблем с часовыми поясами
2. **Добавьте retry-логику** для запросов к внешним API
3. **Логируйте все этапы** сопоставления для отладки
4. **Используйте транзакции БД** при сохранении обогащённых данных
5. **Валидируйте данные** перед сопоставлением (проверка наличия ТС, корректность дат)

### Структура проекта

```
backend/
├── app/
│   ├── adapters/              # Новый модуль
│   │   ├── __init__.py
│   │   ├── base_adapter.py    # Интерфейс IMonitoringAdapter
│   │   └── autograph_adapter.py
│   ├── models/
│   │   └── telemetry_event.py # Новый модуль
│   ├── services/
│   │   ├── transaction_enrichment_service.py  # Новый
│   │   ├── enrichment_scheduler.py            # Новый
│   │   └── fuel_report_service.py            # Новый
│   └── routers/
│       └── card_vehicle_assignment.py         # Новый
```

## 📝 TODO / DONE

### TODO (приоритетные)
- [ ] Создать интерфейс `IMonitoringAdapter`
- [ ] Перенести адаптер "Автограф" из другого проекта
- [ ] Создать модель `CardVehicleAssignment`
- [ ] Реализовать сервис сопоставления транзакций и телеметрии
- [ ] Создать таблицу `enriched_fuel_transactions`
- [ ] Настроить автоматическую обработку новых транзакций

### TODO (средний приоритет)
- [ ] Создать API для управления привязками карт к ТС
- [ ] Добавить frontend для управления привязками
- [ ] Создать дашборд качества сопоставления
- [ ] Реализовать ежедневные отчёты

### DONE ✅
- [x] Модели данных для анализа
- [x] Сервис анализа топливных карт
- [x] API эндпоинты
- [x] Frontend компоненты
- [x] Проектный документ

## 🎯 Критерии успеха этапа 1

1. ✅ Адаптер "Автограф" успешно интегрирован и получает данные
2. ✅ Транзакции автоматически сопоставляются с телеметрией
3. ✅ >80% транзакций успешно обогащаются данными
4. ✅ Система автоматически обрабатывает новые транзакции
5. ✅ Ежедневные отчёты генерируются и отправляются

## 🔮 Подготовка к будущим этапам

### Этап 2 (будущий): Расширение интеграций
- Подключение других систем мониторинга (Wialon, GLONASS)
- Унифицированный интерфейс для всех адаптеров

### Этап 3 (будущий): Интеграция с 1С
- Экспорт данных в формат, совместимый с 1С
- API для получения данных из 1С (если нужно)
- Синхронизация справочников

## 📚 Дополнительные ресурсы

### Полезные Cursor AI промпты

**Для создания адаптера:**
```
Создай класс AutographAdapter, реализующий интерфейс IMonitoringAdapter.
Методы: get_positions, get_odometer, get_refuel_events.
Используй requests с retry, обработку ошибок, логирование.
```

**Для сопоставления:**
```
Создай функцию match_transaction_with_telemetry которая находит 
ближайшее телематическое событие в окне ±15 минут от времени транзакции.
Верни обогащённый объект с координатами, пробегом, флагом подтверждения.
```

**Для автоматизации:**
```
Создай фоновую задачу для APScheduler, которая ежечасно обрабатывает 
новые транзакции: получает телеметрию, сопоставляет, сохраняет результаты.
```

---

**Дата создания:** 2025-01-20  
**Последнее обновление:** 2025-01-20  
**Статус:** В разработке
