# 🔧 Исправление ошибки: ModuleNotFoundError: No module named 'tenacity'

**Проблема:** Контейнер не может запуститься из-за отсутствия модулей `tenacity` и `circuitbreaker`.

**Причина:** Зависимости добавлены в `requirements.txt`, но контейнер был создан до этого.

---

## ✅ РЕШЕНИЕ

### Вариант 1: Пересобрать контейнер (рекомендуется)

```bash
# Остановить контейнер
docker compose down backend

# Пересобрать образ с новыми зависимостями
docker compose build backend

# Запустить контейнер
docker compose up -d backend

# Проверить логи
docker logs gsm_backend --tail 50
```

### Вариант 2: Установить зависимости в запущенном контейнере (временное решение)

```bash
# Установить зависимости
docker exec gsm_backend pip install tenacity==8.2.3 circuitbreaker==2.0.0

# Перезапустить контейнер
docker compose restart backend
```

**⚠️ Внимание:** Вариант 2 работает только до пересборки контейнера. Для постоянного решения используйте Вариант 1.

---

## ✅ ПРОВЕРКА

После пересборки проверьте:

```bash
# Проверить, что модули установлены
docker exec gsm_backend python -c "import tenacity; import circuitbreaker; print('OK')"

# Проверить логи
docker logs gsm_backend --tail 20
```

---

## 📋 ЗАВИСИМОСТИ В requirements.txt

Убедитесь, что в `backend/requirements.txt` есть:

```
tenacity==8.2.3
circuitbreaker==2.0.0
```

Эти зависимости уже добавлены в файл.

---

**Дата:** 2025-12-26  
**Статус:** ✅ Инструкция готова

