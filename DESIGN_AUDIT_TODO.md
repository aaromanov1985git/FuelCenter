# Аудит дизайна проекта GSM - TODO список

**Дата создания:** 2025-01-27  
**Статус:** В процессе

---

## 📋 Общая информация

Этот документ содержит полный список задач для приведения всех компонентов проекта к стандартам дизайна, описанным в `СТАНДАРТЫ_ДИЗАЙНА.md`.

### Легенда статусов:
- ✅ **done** - Соответствует стандартам
- ⚠️ **partial** - Частично соответствует, требует доработки
- ❌ **todo** - Не соответствует стандартам, требует исправления
- 🔄 **in_progress** - В процессе исправления

---

## 🎨 1. ЦВЕТОВАЯ СИСТЕМА

### 1.1 Проверка использования CSS переменных

#### ✅ Стандартные UI компоненты (ui/)
- [x] `Button.jsx` - использует CSS переменные
- [x] `Input.jsx` - использует CSS переменные
- [x] `Select.jsx` - использует CSS переменные
- [x] `Card.jsx` - использует CSS переменные
- [x] `Modal.jsx` - использует CSS переменные
- [x] `Table.jsx` - использует CSS переменные
- [x] `Badge.jsx` - использует CSS переменные
- [x] `Alert.jsx` - использует CSS переменные
- [x] `Toast.jsx` - использует CSS переменные

#### ⚠️ Компоненты с хардкод цветами (требуют исправления)
- [ ] `ConfirmModal.css` - `rgba(0, 0, 0, 0.6)` → использовать переменную
- [ ] `ConfirmModal.css` - `rgba(255, 255, 255, 0.3)` → использовать переменную
- [ ] `UsersList.css` - `rgba(0, 0, 0, 0.3)` → использовать переменную
- [ ] `StatusBadge.css` - хардкод цветов `#fcd34d`, `#f59e0b` → использовать переменные
- [ ] `VehiclesList.css` - `rgba(220, 38, 38, 0.2)` → использовать переменную
- [ ] `Dashboard.css` - множественные `rgba()` → использовать переменные
- [ ] `Dashboard.jsx` - хардкод цветов для графиков `#2196F3`, `#4CAF50` и т.д. → использовать `--color-chart-*`

---

## 📝 2. ТИПОГРАФИКА

### 2.1 Размеры шрифтов

#### ✅ Компоненты, использующие стандартные размеры
- [x] `Button.jsx` - использует `--font-size-sm`, `--font-size-xs`, `--font-size-base`
- [x] `Input.jsx` - использует `--font-size-sm`
- [x] `Card.jsx` - использует стандартные размеры
- [x] `Table.jsx` - использует `--font-size-xs`, `--font-size-sm`

#### ⚠️ Компоненты с фиксированными размерами (требуют проверки)
- [ ] `UploadEventsList.jsx` - `font-size: 0.625rem` → проверить соответствие стандартам
- [ ] `UsersList.jsx` - `font-size: 0.625rem` → проверить соответствие стандартам
- [ ] `TemplatesList.jsx` - проверить использование стандартных размеров
- [ ] `TemplateEditor.jsx` - проверить использование стандартных размеров

### 2.2 Веса шрифтов

#### ✅ Компоненты, использующие стандартные веса
- [x] `Button.jsx` - использует `--font-weight-semibold`
- [x] `Card.jsx` - использует стандартные веса
- [x] `Table.jsx` - использует `--font-weight-semibold`, `--font-weight-normal`

#### ⚠️ Компоненты, требующие проверки
- [ ] Все компоненты списков - проверить использование `--font-weight-*`

---

## 🧩 3. КОМПОНЕНТЫ UI

### 3.1 Карточки (Cards)

#### ✅ Компоненты, использующие стандартный Card
- [x] `UsersList.jsx` - использует `Card` из ui библиотеки
- [x] `UploadEventsList.jsx` - использует `Card` из ui библиотеки
- [x] `TemplatesList.jsx` - использует `Card` из ui библиотеки

#### ✅ Компоненты, использующие стандартный Card
- [x] `VehiclesList.jsx` - использует `Card` из ui библиотеки

#### ❌ Компоненты с кастомными карточками (требуют замены)
- [ ] `Login.jsx` - `.login-card` → использовать `Card`
- [ ] `Register.jsx` - `.register-card` → использовать `Card`
- [ ] `Dashboard.jsx` - проверить использование Card
- [ ] `FuelCardsList.jsx` - проверить использование Card
- [ ] `GasStationsList.jsx` - проверить использование Card
- [ ] `ProvidersList.jsx` - проверить использование Card
- [ ] `TemplateEditor.jsx` - `.template-editor` → использовать `Card`

### 3.2 Кнопки (Buttons)

#### ✅ Компоненты, использующие стандартный Button
- [x] `UsersList.jsx` - использует `Button` из ui библиотеки
- [x] `UploadEventsList.jsx` - использует `Button` из ui библиотеки

#### ✅ Компоненты, использующие стандартный Button
- [x] `VehiclesList.jsx` - использует `Button` из ui библиотеки

#### ❌ Компоненты с кастомными кнопками (требуют замены)
- [ ] `Login.jsx` - `.login-button` → использовать `Button`
- [ ] `Register.jsx` - `.register-button`, `.register-cancel-button` → использовать `Button`
- [ ] `TemplateEditor.jsx` - `.btn-save`, `.btn-cancel`, `.btn-test-connection`, `.btn-load-tables`, `.btn-load-columns` → использовать `Button`
- [ ] `TemplatesList.jsx` - `.btn-add-template` → использовать `Button`
- [ ] `Dashboard.jsx` - `.export-leaders-btn` → использовать `Button`
- [ ] `ConfirmModal.jsx` - `.confirm-modal-button` → использовать `Button`
- [ ] `LoadFirebirdModal.jsx` - проверить использование Button
- [ ] `LoadApiModal.jsx` - проверить использование Button

### 3.3 Инпуты (Inputs)

#### ✅ Компоненты, использующие стандартный Input
- [x] `UsersList.jsx` - использует `Input` из ui библиотеки
- [x] `UploadEventsList.jsx` - использует `Input` из ui библиотеки

#### ✅ Компоненты, использующие стандартный Input
- [x] `VehiclesList.jsx` - использует `Input` из ui библиотеки

#### ⚠️ Компоненты, использующие FormField (требуют проверки)
- [ ] `FormField.jsx` - **НЕ использует** `Input` из ui библиотеки, использует нативный `<input>` → требуется рефакторинг
- [ ] `Login.jsx` - использует `FormField` (который не использует `Input`) → требуется обновление
- [ ] `Register.jsx` - использует `FormField` (который не использует `Input`) → требуется обновление

#### ❌ Компоненты с кастомными инпутами (требуют замены)
- [ ] `TemplateEditor.jsx` - нативные `<input>` → использовать `Input`
- [ ] `TemplatesList.jsx` - нативные `<input>` → использовать `Input`
- [ ] `FuelCardsList.jsx` - проверить использование Input
- [ ] `GasStationsList.jsx` - проверить использование Input
- [ ] `ProvidersList.jsx` - проверить использование Input

### 3.4 Селекты (Selects)

#### ✅ Компоненты, использующие стандартный Select
- [x] `UsersList.jsx` - использует `Select` из ui библиотеки
- [x] `UploadEventsList.jsx` - использует `Select` из ui библиотеки

#### ❌ Компоненты с кастомными селектами (требуют замены)
- [ ] `Register.jsx` - нативный `<select>` → использовать `Select`
- [ ] `TemplateEditor.jsx` - нативные `<select>` → использовать `Select`
- [ ] `TemplatesList.jsx` - нативные `<select>` → использовать `Select`
- [ ] `VehiclesList.jsx` - проверить использование Select
- [ ] `FuelCardsList.jsx` - проверить использование Select
- [ ] `GasStationsList.jsx` - проверить использование Select
- [ ] `ProvidersList.jsx` - проверить использование Select

### 3.5 Таблицы (Tables)

#### ✅ Компоненты, использующие стандартный Table
- [x] `UsersList.jsx` - использует `Table` из ui библиотеки
- [x] `UploadEventsList.jsx` - использует `Table` из ui библиотеки
- [x] `TemplatesList.jsx` - использует `Table` из ui библиотеки

#### ❌ Компоненты с кастомными таблицами (требуют замены)
- [ ] `VehiclesList.jsx` - проверить использование Table
- [ ] `FuelCardsList.jsx` - проверить использование Table
- [ ] `GasStationsList.jsx` - проверить использование Table
- [ ] `ProvidersList.jsx` - проверить использование Table
- [ ] `Dashboard.jsx` - проверить использование Table (если есть)

---

## 🪟 4. МОДАЛЬНЫЕ ОКНА И ПОДТВЕРЖДЕНИЯ

### 4.1 Модальные окна подтверждения

#### ✅ Компоненты, использующие ConfirmModal
- [x] `UsersList.jsx` - использует `ConfirmModal`
- [x] `TemplatesList.jsx` - использует `ConfirmModal`
- [x] `UploadEventsList.jsx` - использует `ConfirmModal` (через кнопку сброса)

#### ⚠️ Компоненты, требующие проверки
- [ ] `ConfirmModal.jsx` - проверить соответствие стандартам (размеры, отступы, стили)
- [ ] `ConfirmModal.jsx` - заменить кастомные кнопки на `Button` из ui библиотеки

### 4.2 Модальные окна форм

#### ✅ Компоненты, использующие стандартный Modal
- [x] `UsersList.jsx` - использует `Modal` из ui библиотеки

#### ❌ Компоненты с кастомными модальными окнами (требуют замены)
- [ ] `LoadFirebirdModal.jsx` - **Использует ConfirmModal.css стили, но не стандартный Modal** → использовать `Modal` из ui библиотеки, заменить нативные input на `Input`, кнопки на `Button`
- [ ] `LoadApiModal.jsx` - **Использует ConfirmModal.css стили, но не стандартный Modal** → использовать `Modal` из ui библиотеки, заменить нативные input на `Input`, textarea на `Input` (type="textarea" если поддерживается), кнопки на `Button`
- [ ] `TemplateEditor.jsx` - проверить, есть ли модальные окна → использовать `Modal`
- [ ] `FuelCardEditModal.jsx` - проверить соответствие стандартам → использовать `Modal`
- [ ] `FilePreviewModal.jsx` - проверить соответствие стандартам → использовать `Modal`

### 4.3 Использование window.confirm() и window.alert()

#### ✅ Компоненты без нативных диалогов
- [x] `TemplatesList.jsx` - заменен `alert()` на `ConfirmModal`

#### ❌ Компоненты с нативными диалогами (требуют замены)
- [ ] Проверить все компоненты на наличие `window.confirm()`
- [ ] Проверить все компоненты на наличие `window.alert()`
- [ ] Проверить все компоненты на наличие `alert()`

---

## 📏 5. СИСТЕМА ОТСТУПОВ

### 5.1 Использование стандартных переменных отступов

#### ✅ Компоненты, использующие стандартные отступы
- [x] `Button.jsx` - использует стандартные padding
- [x] `Input.jsx` - использует стандартные padding
- [x] `Card.jsx` - использует `--padding-card`, `--spacing-*`
- [x] `Modal.jsx` - использует стандартные отступы
- [x] `Table.jsx` - использует стандартные отступы
- [x] `UsersList.jsx` - использует стандартные отступы
- [x] `UploadEventsList.jsx` - использует стандартные отступы

#### ⚠️ Компоненты, требующие проверки
- [ ] `Login.jsx` - проверить использование стандартных отступов
- [ ] `Register.jsx` - проверить использование стандартных отступов
- [ ] `TemplateEditor.jsx` - проверить использование стандартных отступов
- [ ] `TemplatesList.jsx` - проверить использование стандартных отступов
- [ ] `VehiclesList.jsx` - проверить использование стандартных отступов
- [ ] `FuelCardsList.jsx` - проверить использование стандартных отступов
- [ ] `GasStationsList.jsx` - проверить использование стандартных отступов
- [ ] `ProvidersList.jsx` - проверить использование стандартных отступов
- [ ] `Dashboard.jsx` - проверить использование стандартных отступов

### 5.2 Проверка соответствия 8px grid system

#### ⚠️ Компоненты, требующие проверки
- [ ] Все компоненты - проверить, что все отступы кратны 8px
- [ ] Все CSS файлы - проверить отсутствие нестандартных значений (например, 5px, 7px, 13px)

---

## 🔘 6. КНОПКИ

### 6.1 Типы кнопок

#### ✅ Компоненты с правильными типами
- [x] `Button.jsx` - поддерживает все варианты: primary, secondary, success, error, warning, ghost
- [x] `UsersList.jsx` - использует правильные варианты Button
- [x] `UploadEventsList.jsx` - использует правильные варианты Button

#### ⚠️ Компоненты, требующие проверки
- [ ] Все компоненты - проверить использование правильных вариантов кнопок
- [ ] Проверить соответствие цветов кнопок стандартам

### 6.2 Размеры кнопок

#### ✅ Компоненты с правильными размерами
- [x] `Button.jsx` - поддерживает sm, md, lg
- [x] `UsersList.jsx` - использует правильные размеры

#### ⚠️ Компоненты, требующие проверки
- [ ] Все компоненты - проверить использование стандартных размеров кнопок

---

## 📊 7. ТАБЛИЦЫ

### 7.1 Использование стандартного компонента Table

#### ✅ Компоненты, использующие Table
- [x] `UsersList.jsx` - использует `Table` из ui библиотеки
- [x] `UploadEventsList.jsx` - использует `Table` из ui библиотеки
- [x] `TemplatesList.jsx` - использует `Table` из ui библиотеки

#### ✅ Компоненты, использующие стандартный Table
- [x] `VehiclesList.jsx` - использует `Table` из ui библиотеки

#### ❌ Компоненты с кастомными таблицами (требуют замены)
- [ ] `FuelCardsList.jsx` - проверить и заменить на `Table`
- [ ] `GasStationsList.jsx` - проверить и заменить на `Table`
- [ ] `ProvidersList.jsx` - проверить и заменить на `Table`
- [ ] `Dashboard.jsx` - проверить использование Table (если есть)

### 7.2 Стили таблиц

#### ✅ Компоненты с правильными стилями
- [x] `Table.jsx` - использует стандартные стили
- [x] `Table.jsx` - поддерживает striped, hoverable, compact

#### ⚠️ Компоненты, требующие проверки
- [ ] Все компоненты с таблицами - проверить использование стандартных стилей

---

## 📝 8. ФОРМЫ

### 8.1 Компоненты форм

#### ✅ Компоненты, использующие стандартные компоненты
- [x] `Login.jsx` - использует `FormField`
- [x] `Register.jsx` - использует `FormField`
- [x] `UsersList.jsx` - использует `Input`, `Select` из ui библиотеки

#### ⚠️ Компоненты, требующие проверки
- [ ] `FormField.jsx` - проверить, использует ли `Input` из ui библиотеки
- [ ] `TemplateEditor.jsx` - проверить использование стандартных компонентов форм
- [ ] `VehiclesList.jsx` - проверить использование стандартных компонентов форм
- [ ] `FuelCardsList.jsx` - проверить использование стандартных компонентов форм
- [ ] `GasStationsList.jsx` - проверить использование стандартных компонентов форм
- [ ] `ProvidersList.jsx` - проверить использование стандартных компонентов форм

### 8.2 Валидация форм

#### ✅ Компоненты с валидацией
- [x] `Login.jsx` - использует `useFormValidation`
- [x] `Register.jsx` - использует `useFormValidation`
- [x] `UsersList.jsx` - использует `useFormValidation`

#### ⚠️ Компоненты, требующие проверки
- [ ] Все формы - проверить единообразие валидации
- [ ] Все формы - проверить отображение ошибок

---

## 🏷️ 9. СТАТУСНЫЕ ИНДИКАТОРЫ

### 9.1 Бейджи статусов

#### ✅ Компоненты, использующие стандартные бейджи
- [x] `Badge.jsx` - стандартный компонент
- [x] `StatusBadge.jsx` - обертка над Badge

#### ⚠️ Компоненты, требующие проверки
- [ ] `StatusBadge.css` - проверить соответствие стандартам (убрать хардкод цветов)
- [ ] Все компоненты - проверить использование `Badge` или `StatusBadge`

---

## 📱 10. АДАПТИВНОСТЬ

### 10.1 Breakpoints

#### ⚠️ Компоненты, требующие проверки
- [ ] Все компоненты - проверить адаптивность на мобильных устройствах
- [ ] Все компоненты - проверить использование правильных breakpoints
- [ ] Все компоненты - проверить адаптивные отступы

### 10.2 Мобильная версия

#### ⚠️ Компоненты, требующие проверки
- [ ] `Login.jsx` - проверить мобильную версию
- [ ] `Register.jsx` - проверить мобильную версию
- [ ] `UsersList.jsx` - проверить мобильную версию
- [ ] `UploadEventsList.jsx` - проверить мобильную версию
- [ ] `TemplatesList.jsx` - проверить мобильную версию
- [ ] `TemplateEditor.jsx` - проверить мобильную версию
- [ ] Все остальные компоненты - проверить мобильную версию

---

## 🎯 11. СПЕЦИФИЧЕСКИЕ КОМПОНЕНТЫ

### 11.1 Компоненты списков

#### ✅ Компоненты, приведенные к стандартам
- [x] `UsersList.jsx` - использует стандартные компоненты
- [x] `UploadEventsList.jsx` - использует стандартные компоненты
- [x] `TemplatesList.jsx` - использует стандартные компоненты

#### ✅ Компоненты, приведенные к стандартам
- [x] `VehiclesList.jsx` - использует стандартные компоненты (Card, Button, Input, Table)

#### ❌ Компоненты, требующие приведения к стандартам
- [ ] `FuelCardsList.jsx` - заменить на стандартные компоненты
- [ ] `GasStationsList.jsx` - заменить на стандартные компоненты
- [ ] `ProvidersList.jsx` - заменить на стандартные компоненты

### 11.2 Специализированные компоненты

#### ⚠️ Компоненты, требующие проверки
- [ ] `Dashboard.jsx` - проверить соответствие стандартам
- [ ] `CompactDashboard.jsx` - проверить соответствие стандартам
- [ ] `TemplateEditor.jsx` - проверить соответствие стандартам
- [ ] `UploadPeriodLock.jsx` - проверить соответствие стандартам
- [ ] `AdvancedSearch.jsx` - проверить соответствие стандартам
- [ ] `FileUploadProgress.jsx` - проверить соответствие стандартам

### 11.3 Вспомогательные компоненты

#### ⚠️ Компоненты, требующие проверки
- [ ] `Pagination.jsx` - проверить соответствие стандартам (или использовать `Table.Pagination`)
- [ ] `StatusBadge.jsx` - проверить соответствие стандартам
- [ ] `StatusIndicator.jsx` - проверить соответствие стандартам
- [ ] `IconButton.jsx` - проверить соответствие стандартам
- [ ] `FormField.jsx` - проверить, использует ли `Input` из ui библиотеки
- [ ] `EmptyState.jsx` - проверить соответствие стандартам
- [ ] `Breadcrumbs.jsx` - проверить соответствие стандартам
- [ ] `Tooltip.jsx` - проверить соответствие стандартам (или использовать из ui библиотеки)
- [ ] `Toast.jsx` - проверить соответствие стандартам (или использовать из ui библиотеки)
- [ ] `Skeleton.jsx` - проверить соответствие стандартам (или использовать из ui библиотеки)

---

## 🔍 12. ПРОВЕРКА CSS ФАЙЛОВ

### 12.1 Хардкод цветов

#### ❌ Файлы с хардкод цветами (требуют исправления)
- [ ] `ConfirmModal.css` - заменить `rgba()` на переменные
- [ ] `UsersList.css` - заменить `rgba()` на переменные
- [ ] `StatusBadge.css` - заменить hex цвета на переменные
- [ ] `VehiclesList.css` - заменить `rgba()` на переменные
- [ ] `Dashboard.css` - заменить `rgba()` на переменные
- [ ] `Dashboard.jsx` - заменить hex цвета графиков на `--color-chart-*`

### 12.2 Inline стили (требуют замены на CSS классы)

#### ❌ Компоненты с inline стилями (требуют исправления)
- [ ] `LoadFirebirdModal.jsx` - **МНОЖЕСТВЕННЫЕ inline стили** (padding, border, borderRadius, fontSize и т.д.) → использовать стандартные компоненты `Modal`, `Input`, `Button`
- [ ] `LoadApiModal.jsx` - **МНОЖЕСТВЕННЫЕ inline стили** (padding, border, borderRadius, fontSize и т.д.) → использовать стандартные компоненты `Modal`, `Input`, `Button`
- [ ] `UsersList.jsx` - **НЕСКОЛЬКО inline стилей** в tableData (fontWeight, fontSize, color, marginTop) → вынести в CSS классы
- [ ] `UploadEventsList.jsx` - **МНОЖЕСТВЕННЫЕ inline стили** в tableData и статистике (display, gridTemplateColumns, gap, fontSize, fontWeight, color, marginTop, padding, textAlign и т.д.) → вынести в CSS классы
- [ ] Все компоненты - проверить и минимизировать использование inline стилей (приоритет: использовать стандартные компоненты, если возможно)

### 12.3 Хардкод отступов

#### ⚠️ Файлы, требующие проверки
- [ ] Все CSS файлы - проверить отсутствие нестандартных отступов
- [ ] Все CSS файлы - проверить использование переменных отступов
- [ ] Все компоненты - проверить inline стили с отступами (например, `padding: '10px 12px'`)

### 12.4 Хардкод размеров шрифтов

#### ⚠️ Файлы, требующие проверки
- [ ] Все CSS файлы - проверить использование переменных размеров шрифтов
- [ ] Все CSS файлы - проверить отсутствие фиксированных размеров (например, `12px`, `14px`)
- [ ] Все компоненты - проверить inline стили с размерами шрифтов (например, `fontSize: '0.9rem'`)

---

## 📐 13. РАДИУСЫ СКРУГЛЕНИЯ

### 13.1 Использование стандартных радиусов

#### ✅ Компоненты, использующие стандартные радиусы
- [x] `Button.jsx` - использует `--radius-small`
- [x] `Input.jsx` - использует `--radius-small`
- [x] `Card.jsx` - использует `--radius-medium`
- [x] `Modal.jsx` - использует `--radius-large`

#### ❌ Файлы с хардкод радиусами (требуют исправления)
- [ ] `TemplateEditor.css` - `border-radius: 4px` → использовать `--radius-tiny` или `--radius-small`
- [ ] `TemplatesList.css` - `border-radius: 10px` → использовать стандартный радиус
- [ ] `FuelCardEditModal.css` - множественные `border-radius: 4px`, `6px`, `12px` → использовать переменные
- [ ] `AdvancedSearch.css` - `border-radius: 12px` → использовать `--radius-large`
- [ ] `Highlight.css` - `border-radius: 3px` → использовать `--radius-tiny`

#### ⚠️ Компоненты с inline стилями радиусов (требуют исправления)
- [ ] `LoadFirebirdModal.jsx` - inline стили с `borderRadius: '6px'` → использовать `--radius-small`
- [ ] `LoadApiModal.jsx` - inline стили с `borderRadius: '6px'` → использовать `--radius-small`

---

## 🌑 14. ТЕМЫ

### 14.1 Поддержка тем

#### ✅ Компоненты с поддержкой тем
- [x] Все ui компоненты - используют CSS переменные
- [x] `UsersList.jsx` - использует CSS переменные
- [x] `UploadEventsList.jsx` - использует CSS переменные

#### ⚠️ Компоненты, требующие проверки
- [ ] Все компоненты - проверить работу во всех темах (sunrise, midnight, amber-glow)
- [ ] Компоненты с хардкод цветами - исправить для поддержки тем

---

## 📋 15. ПРИОРИТЕТНЫЕ ЗАДАЧИ

### Высокий приоритет (критично для соответствия стандартам)

1. **Замена хардкод цветов на CSS переменные**
   - [ ] `StatusBadge.css` - убрать hex цвета (`#fcd34d`, `#f59e0b`) → использовать `--color-warning-*`
   - [ ] `Dashboard.jsx` - заменить цвета графиков (`#2196F3`, `#4CAF50` и т.д.) на `--color-chart-*`
   - [ ] `ConfirmModal.css` - заменить `rgba(0, 0, 0, 0.6)` и `rgba(255, 255, 255, 0.3)` на переменные
   - [ ] `UsersList.css` - заменить `rgba(0, 0, 0, 0.3)` на переменные
   - [ ] `VehiclesList.css` - заменить `rgba(220, 38, 38, 0.2)` на переменные
   - [ ] `Dashboard.css` - заменить все `rgba()` на переменные

2. **Замена кастомных компонентов на стандартные**
   - [ ] `Login.jsx` - заменить `.login-card` на `Card`, `.login-button` на `Button`
   - [ ] `Register.jsx` - заменить `.register-card` на `Card`, кнопки на `Button`, select на `Select`
   - [ ] `TemplateEditor.jsx` - заменить все кастомные кнопки (`.btn-*`) на `Button`, инпуты на `Input`, селекты на `Select`
   - [ ] `ConfirmModal.jsx` - заменить кастомные кнопки (`.confirm-modal-button`) на `Button`
   - [ ] `LoadFirebirdModal.jsx` - заменить на `Modal`, нативные input на `Input`, кнопки на `Button`
   - [ ] `LoadApiModal.jsx` - заменить на `Modal`, нативные input на `Input`, textarea на `Input`, кнопки на `Button`
   - [ ] `FormField.jsx` - **КРИТИЧНО:** заменить нативный `<input>` на `Input` из ui библиотеки

3. **Замена нативных диалогов**
   - [x] `TemplatesList.jsx` - заменен `alert()` на `ConfirmModal` ✅
   - [ ] Проверить все компоненты на наличие `window.confirm()` → заменить на `ConfirmModal`
   - [ ] Проверить все компоненты на наличие `window.alert()` → заменить на `ConfirmModal` или `Toast`

### Средний приоритет (важно для единообразия)

4. **Приведение списков к стандартам**
   - [x] `VehiclesList.jsx` - уже использует `Card`, `Table`, `Button`, `Input`, `Badge` ✅
   - [ ] `FuelCardsList.jsx` - использовать `Card`, `Table`, `Button`, `Input`, `Select`
   - [ ] `GasStationsList.jsx` - использовать `Card`, `Table`, `Button`, `Input`, `Select`
   - [ ] `ProvidersList.jsx` - использовать `Card`, `Table`, `Button`, `Input`, `Select`

5. **Проверка модальных окон**
   - [ ] `LoadFirebirdModal.jsx` - проверить соответствие стандартам
   - [ ] `LoadApiModal.jsx` - проверить соответствие стандартам
   - [ ] `FuelCardEditModal.jsx` - проверить соответствие стандартам
   - [ ] `FilePreviewModal.jsx` - проверить соответствие стандартам

### Низкий приоритет (улучшения)

6. **Оптимизация и рефакторинг**
   - [ ] Удалить неиспользуемые CSS классы
   - [ ] Объединить дублирующиеся стили
   - [ ] Проверить производительность компонентов

---

## 📊 СТАТИСТИКА ПРОГРЕССА

**Общее количество задач:** ~150+  
**Выполнено:** ~35  
**В процессе:** ~5  
**Осталось:** ~110+

**Процент выполнения:** ~23%

### Детальная статистика по категориям:

#### ✅ Полностью соответствуют стандартам:
- `UsersList.jsx` - использует все стандартные компоненты
- `UploadEventsList.jsx` - использует все стандартные компоненты
- `TemplatesList.jsx` - использует стандартные компоненты
- `VehiclesList.jsx` - использует стандартные компоненты
- Все ui компоненты (`Button`, `Input`, `Select`, `Card`, `Modal`, `Table`, `Badge`, `Alert`, `Toast`)

#### ⚠️ Частично соответствуют (требуют доработки):
- `ConfirmModal.jsx` - структура правильная, но кнопки кастомные
- `LoadFirebirdModal.jsx` - структура правильная, но использует нативные input и кастомные кнопки
- `LoadApiModal.jsx` - структура правильная, но использует нативные input и кастомные кнопки
- `FormField.jsx` - функциональность правильная, но использует нативный input вместо `Input`

#### ❌ Не соответствуют стандартам:
- `Login.jsx` - кастомные стили для карточки и кнопки
- `Register.jsx` - кастомные стили для карточки, кнопок и select
- `TemplateEditor.jsx` - множество кастомных кнопок и инпутов
- `FuelCardsList.jsx` - не использует стандартные компоненты
- `GasStationsList.jsx` - не использует стандартные компоненты
- `ProvidersList.jsx` - не использует стандартные компоненты
- `Dashboard.jsx` - хардкод цветов для графиков
- `StatusBadge.css` - хардкод цветов

---

## 📝 ЗАМЕТКИ

### Известные проблемы:

#### Критичные проблемы:
1. **`FormField.jsx`** - использует нативный `<input>` вместо `Input` из ui библиотеки - требуется рефакторинг
2. **`Dashboard.jsx`** - содержит хардкод цветов для графиков (`#2196F3`, `#4CAF50` и т.д.) - требуется замена на `--color-chart-*`
3. **`StatusBadge.css`** - содержит хардкод цветов (`#fcd34d`, `#f59e0b`) - требуется замена на CSS переменные
4. **`LoadFirebirdModal.jsx`** и **`LoadApiModal.jsx`** - используют множественные inline стили и нативные input вместо стандартных компонентов

#### Средние проблемы:
5. Множество компонентов используют кастомные стили вместо стандартных компонентов:
   - `Login.jsx`, `Register.jsx` - кастомные карточки и кнопки
   - `TemplateEditor.jsx` - множество кастомных кнопок и инпутов
   - `FuelCardsList.jsx`, `GasStationsList.jsx`, `ProvidersList.jsx` - не используют стандартные компоненты
6. Некоторые компоненты не поддерживают все темы из-за хардкод цветов
7. Множество inline стилей в компонентах (особенно в `UploadEventsList.jsx`, `UsersList.jsx`)
8. Хардкод радиусов в CSS файлах (`4px`, `6px`, `10px`, `12px` вместо переменных)

### Рекомендации по приоритетам:

#### Этап 1: Критичные исправления (1-2 дня)
1. **Заменить хардкод цветов на CSS переменные:**
   - `StatusBadge.css` - заменить hex цвета
   - `Dashboard.jsx` - заменить цвета графиков на `--color-chart-*`
   - `ConfirmModal.css`, `UsersList.css`, `VehiclesList.css`, `Dashboard.css` - заменить `rgba()` на переменные

2. **Рефакторинг FormField:**
   - Обновить `FormField.jsx` для использования `Input` из ui библиотеки
   - Обновить все компоненты, использующие `FormField` (если потребуется)

#### Этап 2: Замена кастомных компонентов (3-5 дней)
3. **Заменить модальные окна:**
   - `LoadFirebirdModal.jsx` → использовать `Modal`, `Input`, `Button`
   - `LoadApiModal.jsx` → использовать `Modal`, `Input`, `Button`
   - `ConfirmModal.jsx` → заменить кнопки на `Button`

4. **Заменить формы:**
   - `Login.jsx` → использовать `Card`, `Button`, `Input`
   - `Register.jsx` → использовать `Card`, `Button`, `Input`, `Select`

5. **Заменить списки:**
   - `FuelCardsList.jsx` → использовать стандартные компоненты
   - `GasStationsList.jsx` → использовать стандартные компоненты
   - `ProvidersList.jsx` → использовать стандартные компоненты

#### Этап 3: Оптимизация и рефакторинг (2-3 дня)
6. **Убрать inline стили:**
   - `UploadEventsList.jsx` - вынести inline стили в CSS классы
   - `UsersList.jsx` - вынести inline стили в CSS классы
   - Все остальные компоненты - минимизировать inline стили

7. **Заменить хардкод радиусов:**
   - Все CSS файлы - заменить фиксированные радиусы на переменные

8. **Финальная проверка:**
   - Провести тестирование во всех темах (sunrise, midnight, amber-glow)
   - Проверить адаптивность на мобильных устройствах
   - Проверить производительность компонентов

### Процесс работы:
1. Выбрать задачу из списка
2. Выполнить изменения
3. Протестировать во всех темах
4. Обновить статус в этом документе (✅ done)
5. Перейти к следующей задаче

---

**Последнее обновление:** 2025-01-27  
**Версия:** 1.0
