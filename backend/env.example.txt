# ============================================================================
# GSM Converter - Пример конфигурации
# ============================================================================
# КРИТИЧНО: Перед запуском в production ОБЯЗАТЕЛЬНО:
# 1. Скопируйте этот файл в .env
# 2. Замените все значения на безопасные
# 3. Установите ENVIRONMENT=production
# ============================================================================

# ----------------------------------------------------------------------------
# Окружение и режим работы
# ----------------------------------------------------------------------------
ENVIRONMENT=development
# В production установите:
# ENVIRONMENT=production

DEBUG=false
LOG_LEVEL=INFO

# ----------------------------------------------------------------------------
# База данных
# ----------------------------------------------------------------------------
DATABASE_URL=postgresql://gsm_user:gsm_password@localhost:5432/gsm_db

# ----------------------------------------------------------------------------
# 🔴 КРИТИЧЕСКИЕ НАСТРОЙКИ БЕЗОПАСНОСТИ
# ----------------------------------------------------------------------------

# JWT Secret Key - ОБЯЗАТЕЛЬНО замените!
# Сгенерируйте командой: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=your-secret-key-here-change-in-production

# Пароль администратора по умолчанию - ОБЯЗАТЕЛЬНО замените!
# Сгенерируйте командой: python -c "import secrets; print(secrets.token_urlsafe(16))"
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=admin@example.com

# Ключ шифрования для чувствительных данных (пароли Firebird, API ключи)
# Сгенерируйте командой: python -c "import secrets; print(secrets.token_urlsafe(32))"
ENCRYPTION_KEY=

# ----------------------------------------------------------------------------
# Аутентификация
# ----------------------------------------------------------------------------
ENABLE_AUTH=true
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=30

# ----------------------------------------------------------------------------
# Rate Limiting (защита от DDoS)
# ----------------------------------------------------------------------------
ENABLE_RATE_LIMIT=true
RATE_LIMIT_DEFAULT=100/minute
RATE_LIMIT_STRICT=10/minute

# ----------------------------------------------------------------------------
# CORS (разрешённые домены)
# ----------------------------------------------------------------------------
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ----------------------------------------------------------------------------
# Уведомления - Email (опционально)
# ----------------------------------------------------------------------------
EMAIL_ENABLED=false
EMAIL_SMTP_HOST=smtp.example.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER=
EMAIL_SMTP_PASSWORD=
EMAIL_FROM_ADDRESS=
EMAIL_FROM_NAME=GSM Converter
EMAIL_USE_TLS=true

# ----------------------------------------------------------------------------
# Уведомления - Telegram (опционально)
# ----------------------------------------------------------------------------
TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN=

# ----------------------------------------------------------------------------
# Уведомления - Push (опционально)
# ----------------------------------------------------------------------------
PUSH_ENABLED=false
PUSH_VAPID_PUBLIC_KEY=
PUSH_VAPID_PRIVATE_KEY=
PUSH_VAPID_SUBJECT=

# ----------------------------------------------------------------------------
# Загрузка файлов
# ----------------------------------------------------------------------------
MAX_UPLOAD_SIZE=52428800

