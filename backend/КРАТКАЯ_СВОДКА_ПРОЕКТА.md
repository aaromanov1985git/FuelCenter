# Краткая сводка: Анализ топливных карт

## 🎯 Цель этапа 1

Сопоставить транзакции по топливным картам с данными из системы мониторинга "Автограф Техноком" для:
- Подтверждения факта заправки
- Определения местоположения ТС в момент транзакции
- Расчёта пробега между заправками
- Выявления расхождений

## ✅ Что уже есть

- Транзакции от АЗС (загружаются в систему)
- Модели данных для анализа
- Сервис анализа топливных карт
- API и Frontend для просмотра результатов
- Готовые методы работы с "Автограф" (в другом проекте)

## 🔨 Что нужно сделать

### Приоритет 1 (критично)
1. **Перенести адаптер "Автограф"** из другого проекта
2. **Создать справочник "Карта → ТС"** с историей привязок
3. **Реализовать сопоставление** транзакций и телеметрии

### Приоритет 2 (важно)
4. **Создать хранилище** для обогащённых транзакций
5. **Автоматизировать обработку** новых транзакций
6. **Добавить логирование** процесса сопоставления

### Приоритет 3 (желательно)
7. **Создать дашборд** качества сопоставления
8. **Настроить ежедневные отчёты**

## 📋 Быстрый план на неделю

| День | Задача |
|------|--------|
| 1 | Перенести адаптер "Автограф", проверить подключение |
| 2 | Создать модель CardVehicleAssignment, миграция БД |
| 3 | Реализовать функцию сопоставления, тест на выборке |
| 4 | Создать таблицу enriched_fuel_transactions |
| 5 | Настроить автоматическую обработку + отчёт |

## 🔧 Ключевые компоненты

### 1. Интерфейс адаптера
```python
class IMonitoringAdapter:
    - get_positions(vehicle_id, start, end)
    - get_odometer(vehicle_id, timestamp)
    - get_refuel_events(vehicle_id, period)
```

### 2. Унифицированная модель
```python
TelemetryEvent:
    - vehicle_id, timestamp, lat, lon
    - odometer_km, event_type
    - source_system
```

### 3. Алгоритм сопоставления
```
Транзакция → ТС (по карте) → Телеметрия (±15 мин) → 
Ближайшее событие → Расстояние до АЗС → Пробег → 
Результат обогащения
```

## 📁 Структура файлов

```
backend/
├── app/
│   ├── adapters/
│   │   ├── base_adapter.py          # Интерфейс
│   │   └── autograph_adapter.py    # Реализация
│   ├── models.py                    # + CardVehicleAssignment
│   ├── services/
│   │   └── transaction_enrichment_service.py
│   └── routers/
│       └── card_vehicle_assignment.py
```

## 🎯 Критерии успеха

- ✅ Адаптер "Автограф" получает данные
- ✅ >80% транзакций успешно обогащаются
- ✅ Автоматическая обработка работает
- ✅ Ежедневные отчёты генерируются

## 📚 Документы

- **ПЛАН_РЕАЛИЗАЦИИ_АНАЛИЗА_ТОПЛИВНЫХ_КАРТ.md** — детальный план задач
- **АРХИТЕКТУРА_ИНТЕГРАЦИИ_МОНИТОРИНГА.md** — техническая архитектура
- **ПРОЕКТИРОВАНИЕ_СЕРВИСА_АНАЛИЗА_ТОПЛИВНЫХ_КАРТ.md** — общее описание

---

**Статус:** Проектирование завершено, готов к реализации
