# Инструкция по проверке работы XML API с сертификатом

## Способ 1: Проверка через UI (Рекомендуется)

### Шаг 1: Создание/редактирование шаблона
1. Откройте интерфейс создания/редактирования шаблона
2. Выберите тип подключения: **"Веб-сервис (web)"**
3. Заполните обязательные поля:
   - **Базовый URL**: `http://176.222.217.51:1342` (или ваш базовый URL)
   - **Сертификат (Certificate)**: `545.1AFB41693CD79C72796D7B56F2D727B8B343BF17` (или ваш сертификат)
   - **Код POS (POS Code)**: `23` (или ваш код)
   - **Endpoint для получения транзакций** (опционально): `http://176.222.217.51:1342/sncapi/sale` (если отличается от BASE_URL/sncapi/sale)

### Шаг 2: Проверка подключения
1. Нажмите кнопку **"🔍 Проверить подключение"**
2. Должно появиться сообщение об успешном подключении
3. Если есть ошибка, проверьте:
   - Правильность базового URL
   - Правильность сертификата
   - Доступность сервера

### Шаг 3: Сохранение шаблона
1. Заполните остальные обязательные поля (маппинг полей)
2. Сохраните шаблон

## Способ 2: Проверка через API (для разработчиков)

### Тест подключения (без сохранения шаблона)

```bash
POST /api/v1/templates/test-api-connection?connection_type=web
Content-Type: application/json

{
  "base_url": "http://176.222.217.51:1342",
  "certificate": "545.1AFB41693CD79C72796D7B56F2D727B8B343BF17",
  "pos_code": 23,
  "endpoint": "http://176.222.217.51:1342/sncapi/sale"
}
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "message": "Подключение успешно",
  "status": "ok",
  "checked_at": "2025-01-15T10:30:00Z"
}
```

### Тест подключения для существующего шаблона

```bash
POST /api/v1/templates/{template_id}/test-api-connection
```

### Загрузка транзакций через API

```bash
POST /api/v1/transactions/load-from-api?template_id={template_id}&date_from=2025-01-01&date_to=2025-01-15
```

**Параметры:**
- `template_id` - ID сохраненного шаблона
- `date_from` - Начальная дата (формат: YYYY-MM-DD)
- `date_to` - Конечная дата (формат: YYYY-MM-DD)
- `card_numbers` (опционально) - Список номеров карт через запятую

**Пример:**
```bash
POST /api/v1/transactions/load-from-api?template_id=1&date_from=2025-01-01&date_to=2025-01-15&card_numbers=3000000110013446,3000000100000227
```

## Способ 3: Проверка через логи

### Просмотр логов Docker контейнера

```bash
# Просмотр логов backend
docker logs gsm_backend -f

# Или если контейнер называется по-другому
docker ps  # найти имя контейнера
docker logs <container_name> -f
```

### Что искать в логах:

1. **При проверке подключения:**
   ```
   XML API ПАРАМЕТРЫ ОБНАРУЖЕНЫ:
     Сертификат (certificate): 545.1AFB41693CD79C72796D7B56F2D727B8B343BF17
     POS Code: 23
     → Используется XML API с сертификатом (авторизация не требуется)
   ```

2. **При загрузке транзакций:**
   ```
   XML ЗАПРОС ДЛЯ ПОЛУЧЕНИЯ ТРАНЗАКЦИЙ:
   <RequestDS>
     <Request>
       <Command>getsale</Command>
       <Version>1</Version>
       <Certificate>545.1AFB41693CD79C72796D7B56F2D727B8B343BF17</Certificate>
       <POSCode>23</POSCode>
     </Request>
     <Card>
       <CardNumber>3000000110013446</CardNumber>
       <StartDate>2025-01-01 00:00:00</StartDate>
       <EndDate>2025-01-15 23:59:59</EndDate>
     </Card>
   </RequestDS>
   ```

3. **Успешный ответ:**
   ```
   Получено транзакций из XML API: 10
   ```

4. **Ошибки:**
   - `405 Method Not Allowed` - неправильный endpoint
   - `403 Forbidden` - неправильный сертификат
   - `400 Bad Request` - неправильный формат запроса

## Способ 4: Проверка через Swagger UI

1. Откройте в браузере: `http://localhost:8000/docs`
2. Найдите endpoint: `POST /api/v1/templates/test-api-connection`
3. Нажмите "Try it out"
4. Заполните параметры:
   ```json
   {
     "base_url": "http://176.222.217.51:8080",
     "certificate": "545.1AFB41693CD79C72796D7B56F2D727B8B343BF17",
     "pos_code": 23
   }
   ```
5. Нажмите "Execute"
6. Проверьте ответ

## Возможные проблемы и решения

### Проблема: "Для XML API требуется указать сертификат"
**Решение:** Убедитесь, что в настройках подключения указан сертификат в поле "Сертификат (Certificate)"

### Проблема: "405 Method Not Allowed"
**Решение:** 
- Проверьте правильность базового URL
- Убедитесь, что endpoint `/sncapi/sale` доступен
- Проверьте логи для деталей

### Проблема: "Не удалось подключиться к XML API endpoint"
**Решение:**
- Проверьте доступность сервера: `curl http://176.222.217.51:8080/sncapi/sale`
- Проверьте правильность сертификата
- Проверьте правильность POS кода

### Проблема: "Ошибка парсинга XML ответа"
**Решение:**
- Проверьте логи для просмотра сырого XML ответа
- Убедитесь, что сервер возвращает корректный XML

## Проверка загруженных транзакций

После успешной загрузки транзакций:

1. Откройте раздел "Транзакции" в интерфейсе
2. Проверьте наличие загруженных транзакций
3. Проверьте корректность данных:
   - Номер карты
   - Дата и время
   - Название топлива
   - Объем
   - Сумма
   - Номер АЗС

## Тестовые данные

Для тестирования можно использовать:
- **Базовый URL**: `http://176.222.217.51:1342`
- **Endpoint для транзакций**: `http://176.222.217.51:1342/sncapi/sale`
- **Сертификат**: `545.1AFB41693CD79C72796D7B56F2D727B8B343BF17` (из примера)
- **POS Code**: `23`
- **Номера карт**: `3000000110013446`, `3000000100000227` (из примера)
- **Период**: последние 7-30 дней
