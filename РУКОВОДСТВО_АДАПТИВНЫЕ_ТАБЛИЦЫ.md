# Руководство по адаптивным таблицам

## 📋 Обзор

Все таблицы в проекте автоматически адаптируются под размер экрана:
- **Мобильные (< 768px)**: карточный вид (каждая строка как карточка)
- **Планшеты (768px - 1024px)**: табличный вид с горизонтальной прокруткой
- **Десктоп (> 1024px)**: полная таблица

## ✅ Правильное использование

### Обязательный атрибут `data-label`

Для корректной работы карточного вида на мобильных устройствах **каждая ячейка `<td>` должна иметь атрибут `data-label`** с названием колонки:

```jsx
<table className="data-table">
  <thead>
    <tr>
      <th>ID</th>
      <th>Название</th>
      <th>Статус</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td data-label="ID">123</td>
      <td data-label="Название">Пример</td>
      <td data-label="Статус">Активен</td>
    </tr>
  </tbody>
</table>
```

### Пример из реального компонента

```jsx
// src/components/ProvidersList.jsx
<td data-label="ID">{provider.id}</td>
<td data-label="Название">{provider.name}</td>
<td data-label="Код">{provider.code}</td>
<td data-label="Организация">{orgName}</td>
<td data-label="Статус">
  <Badge variant={provider.is_active ? 'success' : 'default'}>
    {provider.is_active ? 'Активен' : 'Неактивен'}
  </Badge>
</td>
<td data-label="Действия">
  {/* Кнопки действий */}
</td>
```

## 🎨 Как это работает

### На мобильных устройствах (< 768px)

1. Таблица преобразуется в карточки
2. Каждая строка `<tr>` становится отдельной карточкой
3. Заголовки `<thead>` скрываются
4. Ячейки `<td>` отображаются вертикально с метками из `data-label`

**CSS автоматически:**
- Скрывает `<thead>`
- Преобразует `<tr>` в блоки
- Добавляет метки через `::before` из `data-label`
- Применяет правильные отступы и стили

### На планшетах (768px - 1024px)

- Таблица остается в табличном виде
- Добавляется горизонтальная прокрутка при необходимости
- Компактные отступы и размеры шрифтов

### На десктопе (> 1024px)

- Полная таблица со всеми функциями
- Все колонки видны
- Нормальные отступы и размеры

## 📝 Чеклист при создании новой таблицы

- [ ] Обернуть таблицу в `<div className="table-wrapper">`
- [ ] Использовать класс `data-table` для `<table>`
- [ ] Добавить `data-label` на каждую `<td>` с текстом заголовка колонки
- [ ] Протестировать на мобильном устройстве (< 768px)
- [ ] Проверить, что метки отображаются корректно
- [ ] Убедиться, что текст не обрезается

## 🔧 Использование UI компонента Table

Компонент `Table` из UI библиотеки автоматически добавляет `data-label`:

```jsx
import { Table } from './ui'

<Table
  columns={[
    { key: 'id', header: 'ID' },
    { key: 'name', header: 'Название' },
    { key: 'status', header: 'Статус' }
  ]}
  data={items}
/>
```

Компонент автоматически:
- Добавляет `data-label={column.header}` на каждую ячейку
- Поддерживает все адаптивные стили
- Работает на всех устройствах

## ⚠️ Частые ошибки

### ❌ НЕПРАВИЛЬНО
```jsx
<td>{value}</td>  // Нет data-label
```

### ✅ ПРАВИЛЬНО
```jsx
<td data-label="Колонка">{value}</td>
```

### ❌ НЕПРАВИЛЬНО
```jsx
<td data-label={undefined}>  // Пустой data-label
```

### ✅ ПРАВИЛЬНО
```jsx
<td data-label="ID">{id}</td>
```

## 🎯 Примеры компонентов

Следующие компоненты уже правильно используют адаптивные таблицы:
- ✅ `App.jsx` (таблица транзакций)
- ✅ `ProvidersList.jsx`
- ✅ `VehiclesList.jsx`
- ✅ `GasStationsList.jsx`
- ✅ `FuelCardsList.jsx`
- ✅ `TemplatesList.jsx`
- ✅ `UsersList.jsx`
- ✅ UI компонент `Table`

## 🔄 Миграция существующих таблиц

Если нужно обновить существующую таблицу:

1. Найти все `<td>` элементы
2. Добавить `data-label` с текстом заголовка
3. Протестировать на мобильном

**Пример миграции:**

```jsx
// До
<td>{item.name}</td>

// После
<td data-label="Название">{item.name}</td>
```

## 📱 Тестирование

Для проверки адаптивности:

1. Откройте DevTools (F12)
2. Включите режим устройства (Ctrl+Shift+M)
3. Выберите размер экрана < 768px (например, iPhone 12)
4. Проверьте, что таблица преобразовалась в карточки
5. Убедитесь, что метки `data-label` отображаются корректно

## 🎨 Кастомизация

Стили адаптивных таблиц находятся в:
- `src/App.css` (основные стили `.data-table`)
- `src/components/ui/Table/Table.css` (UI компонент)

Для кастомизации используйте CSS переменные и существующие классы.
